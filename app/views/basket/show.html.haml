%h2 Basket

%table.basket
  - current_basket.lines.each do |line|
    %tr
      %td= line.title
      %td= line.quantity
      %td= line.price
      %td= button_to 'Remove', basket_remove_path(:item => line.item), :method => :delete

